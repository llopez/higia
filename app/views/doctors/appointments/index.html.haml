.card
  .card-header
    Listado de Turnos
  .card-body
    %table.table.table-small
      %thead
        %tr
          %th= Appointment.human_attribute_name :date
          %th
      %tbody
        = render @appointments

    = link_to "Nuevo Turno", new_doctors_appointment_path, class: 'btn btn-primary'